# nixpacks.toml — tells Railway to install FFmpeg (needed for video generation)
[phases.setup]
nixPkgs = ["ffmpeg", "python310", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd backend && python api_server.py"
